services:
  api:
    build: .
    container_name: experimentation_engine_api
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./app.db
      - LOG_LEVEL=INFO
      - API_BASE_URL=http://localhost:8000
    volumes:
      - ./app.db:/app/app.db